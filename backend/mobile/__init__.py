"""Mobile + expansion domain (Phase 10).

Mobile-optimized backend: a server-side watchlist with offline-sync conflict
resolution, device-token registry + FCM push, per-user notification
preferences, and a single aggregated ``/mobile/home`` payload that collapses the
dashboard into one battery-friendly round-trip.
"""
